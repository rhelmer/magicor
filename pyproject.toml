[project]
name = "magicor"
version = "1.1"
description = "2D platform game (pygame)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.5",
    "pillow>=10",
]

[tool.uv]
package = false
